# Responsible for conflict resolution - takes paths to two conflicting files
# and return a new file which is considered as newest version.
# If return value is None, confilct resolution is considered as failed, 
def resolve_conflict(file1, file2):
    print(file1, file2)
    return "XXX"